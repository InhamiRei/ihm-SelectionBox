import ihm_SelectionBox from "./components/selectionBox.js";

function ihmSelectionBox(config) {
  return new ihm_SelectionBox(config);
}

window.ihmSelectionBox = ihmSelectionBox;
export default ihmSelectionBox;
